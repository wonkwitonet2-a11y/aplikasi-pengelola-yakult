--- src/components/ManagerView.tsx
+++ src/components/ManagerView.tsx
@@ -941,23 +941,2 @@
 
-  // Auto-fill baseline for active YLs
-  const handleAutoFillBaselineBreakdown = () => {
-    if (activeYLsList.length === 0) {
-      setBreakdownMsg("⚠️ Tidak ada YL aktif untuk diisi.");
-      return;
-    }
-    const newMap = { ...activeGridMap };
-    activeYLsList.forEach(yl => {
-      if (!newMap[yl.area]) {
-        newMap[yl.area] = { pembagiTanggal: 25, days: {} };
-      }
-      const planVal = yl.rata2 > 0 ? yl.rata2 : (yl.targetYL > 0 ? yl.targetYL : 200);
-      for (let i = 1; i <= 31; i++) {
-        if (!newMap[yl.area].days[i]) newMap[yl.area].days[i] = { yo: 0, om: 0, os: 0, yt: 0 };
-        // We just do flat YO mapping for auto baseline, om/os/yt set to 0.
-        newMap[yl.area].days[i].yo = Math.trunc(planVal);
-      }
-    });
-    setActiveGridMap(newMap);
-    setBreakdownMsg(`✨ Data baseline harian otomatis diisikan untuk ${gridSubMode === "BD" ? "Plan Rencana" : "Realisasi"}!`);
-    setTimeout(() => setBreakdownMsg(""), 3000);
-  };
